// Prisma Studio Configuration
module.exports = {
  // Custom configuration for Prisma Studio
  port: 5555,
  host: 'localhost',
  // Enable CORS for development
  cors: {
    origin: ['http://localhost:3000'],
    credentials: true
  }
};
